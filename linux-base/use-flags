# In dieser Datei stehen die Use-Flags, mit der alle Programme unter Gentoo kompiliert wurden.
# (eine vollstaendige Liste gibts unter /usr/portage/profiles/use.desc)

"alsa apache2 crypt curl dga directfb gd gif java jpeg nocd oggvorbis perl png snmp ssl tiff truetype X xml xml2 -oss -apm -arts -cups -foomaticdb -gdbm -gpm -kde -gnome"
